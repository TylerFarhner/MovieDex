<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel='stylesheet' href='/css/style.css' />
  <title>MovieDex</title>
</head>

<body>

  <h2>Enter a New Movie</h2>

  <nav>
    <a href="../">Home</a>
    <a href="/movies">Library</a>
  </nav>

  <form action="/movie/<%= movie._id %>?_method=PUT" method="POST" autocomplete="off">
    <label>Title:
      <input type="text" name="title" placeholder="<%= movie.title %>">
    </label>

    <label>Release Year:
      <input type="number" name="release" placeholder="<%= movie.release %>">
    </label>

    <label>Rating
      <select name="rating">
        <option value="<%= movie.rating %>"><%= movie.rating %></option>
        <option value="G">G</option>
        <option value="PG">PG</option>
        <option value="PG-13">PG-13</option>
        <option value="R">R</option>
      </select>
    </label>

    <label>Cast:
      <input type="text" name="cast" placeholder="<%= movie.cast %>">
    </label>

    <input type="submit" value="Update!">
  </form>

</body>

</html>